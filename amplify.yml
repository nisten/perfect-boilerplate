version: 0.1
frontend:
  phases:
    build:
      commands:
        - yarn install
        - yarn test
        - yarn build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*