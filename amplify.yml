version: 0.1
frontend:
  phases:
    build:
      commands:
        - yarn install
        - yarn test
        - gatsby build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
